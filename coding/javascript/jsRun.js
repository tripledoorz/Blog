
/**
 * js运行机制
 * 任务队列：同步、异步（settimeout/dom事件/promise）
 * eventloop：事件循环
 * 
 */

console.log(1);
setTimeout(() => {
    console.log(2);
}, 0);
console.log(3);
 for (let index = 0; index < array.length; index++) {
    const element = array[index];
}
 
